@model Partida

@{
    ViewData["Title"] = "Create";
}
<h2>Crear Partida</h2> <div class="container" style="max-width: 600px;"> <form asp-action="Create" method="post"> <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
<div class="mb-3">
        <label asp-for="Jugador1Id" class="form-label"></label>
        <select asp-for="Jugador1Id" class="form-select" asp-items="ViewBag.Jugador1Id"></select>
    </div>

    <div class="mb-3">
        <label asp-for="Jugador2Id" class="form-label"></label>
        <select asp-for="Jugador2Id" class="form-select" asp-items="ViewBag.Jugador2Id"></select>
    </div>

    <div class="mb-3">
        <label asp-for="TurnoJugadorId" class="form-label"></label>
        <select asp-for="TurnoJugadorId" class="form-select" asp-items="ViewBag.TurnoJugadorId"></select>
    </div>

    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary">Guardar</button>
        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
    </div>
</form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
